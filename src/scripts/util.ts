export const PLAYER_SIZE = 50;
export const STUMP_SIZE = 50;
export const ROPE_THICKNESS = 5;

export const DEG_TO_RAD = Math.PI / 180;

export const delay = ms => new Promise(res => setTimeout(res, ms));